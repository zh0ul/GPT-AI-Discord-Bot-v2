Any files in templates_custom override files found in templates.

Copy any file from templates to here then change.

While no update will ever overwrite your files in templates_custom, changes to code 
might render your custom template broken.  If this happens, simply copy the 
new template from templates to templates_custom and modify again.
